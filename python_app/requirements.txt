zstandard
pycryptodome
# CA bundle so the embedded CPython can verify HTTPS to the roster providers.
certifi
# pyaes and retro_roster_patcher are vendored into python_app/ (no wheel on
# Android's --only-binary install), so they resolve by import, not pip.
# retro_roster_patcher's optional [images] extra (Pillow, for WE2002 kit/TIM
# art) is intentionally omitted; roster patching works without it.
